# Services definition for building & deploying

name: docker-pdftotext

services:

  pdftotext:
    # Build
    image: ${IMAGES_PREFIX:-app}:${IMAGES_TAG:-latest}
    build:
      context: .
      dockerfile: ./Dockerfile
      target: app_prod
